# Minimal Python dependencies for OCONNECT
# Core web framework
Flask==2.2.5

# Environment variable loader (optional but used in app.py)
python-dotenv==1.0.0

# Optional realtime support (only if you enable Socket.IO features)
Flask-SocketIO==5.3.2
eventlet==0.33.3

# Presentation generator used for project slides
python-pptx==0.6.21

# Image handling required by python-pptx
Pillow>=9.5,<11

# Security / utilities (Flask pulls these in, but pinning helps reproducible installs)
itsdangerous==2.1.2
Werkzeug==2.2.3
Jinja2==3.1.2

# Production WSGI server (optional for deployment)
gunicorn==20.1.0


